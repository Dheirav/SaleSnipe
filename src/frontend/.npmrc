# react-scripts@5.0.1 declares a peerOptional dependency on typescript ^3.2.1 || ^4,
# but this app is on typescript ^5. The combination works; npm's strict peer
# resolution is what objects. Without this, `npm ci` and `npm install` both fail
# here with ERESOLVE.
legacy-peer-deps=true
